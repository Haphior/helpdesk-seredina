{
  "name": "@seredina/ai-adapters",
  "private": true,
  "version": "0.0.0",
  "description": "LlmProviderAdapter interface + implementations (Anthropic, and a deterministic test double).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.126.0"
  },
  "devDependencies": {
    "@seredina/config": "*",
    "typescript": "^5.6.3",
    "vitest": "^3.2.4"
  }
}
