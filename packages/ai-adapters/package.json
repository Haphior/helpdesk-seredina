{
  "name": "@seredina/ai-adapters",
  "private": true,
  "version": "0.0.0",
  "description": "LlmProviderAdapter interface + implementations (Anthropic, OpenAI, local via Ollama, and a deterministic test double).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.126.0",
    "@huggingface/transformers": "^4.3.0",
    "openai": "^6.49.0"
  },
  "devDependencies": {
    "@seredina/config": "*",
    "typescript": "^5.6.3",
    "vitest": "^5.0.1"
  }
}
