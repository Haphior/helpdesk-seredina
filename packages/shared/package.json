{
  "name": "@seredina/shared",
  "private": true,
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@seredina/config": "*",
    "typescript": "^5.6.3",
    "vitest": "^5.0.1"
  }
}
